---
framework: mocha
postMerge:
  pattern:
    - "./_test_/**/*.spec.ts"
  strategy:
    threshold: <5|100s|30m|6h>
    type: <commit|time>
preMerge:
  pattern:
    - "./_test_/**/*.spec.ts"
preRun:
  - npm ci
postRun:
  - node --version
version: 2.0
